# Long Task Runtime 优化 Review 结果

基于对代码的逐项检查，以下是对照文档 `docs/long-task-runtime-optimization.md` 的审查结论。

---

## Findings

### Finding 1 — Repeat Read Guard 实现缺失

文档 P1 明确要求：「同一任务重复全文读取同一文件时，返回已读证据和建议 range，而不是再次返回全文。」文档第 13 落地状态也声称「Repeat Read Guard」已完成。

但我在整个 `bridge/` 中找不到任何相关的 guard 逻辑：

- `bridge/editing/readRuntime.mjs` 不包含任何 guard / repeat / duplicate / already / evidence / mtime 相关逻辑，只有纯粹的行号解析和切片。
- `bridge/tools.mjs` 中 `read_file` / `read_block` 的 run 实现中没有对 `autoToolEvidence` 做读取前检查。
- 没有 `isFullRead`、`repeatFullRead`、`fullReadGuard` 等函数存在。

**风险**：压缩后模型仍可能重复读取同一完整文件，这正是优化方案要解决的核心问题之一。文档声称已落地，但代码中未找到对应实现。

### Finding 2 — `appendRuntimeToolEvidenceToSystemPrompt` 在 `tools.mjs` 中未定义

`providers.mjs` 第 2 行从 `tools.mjs` 导入 `appendRuntimeToolEvidenceToSystemPrompt`，但 `tools.mjs` 中搜索该函数名返回 0 结果。只有 `buildToolEvidenceSummary` 存在。

可能情况：
- 该函数在 `tools.mjs` 中以不同的导出名存在（搜索可能遗漏了合并导出路径）。
- 或者这是一个编译/模块解析问题。

**风险**：如果该导入解析失败，provider 层将无法在 system prompt 中注入 tool evidence，压缩后模型会丢失所有工具执行记录。

### Finding 3 — `agentPrompting.mjs` 中缺少 progress ledger / tool evidence 注入

文档 P1 要求「压缩后 system prompt 必须注入 progress ledger」。在 `agentPrompting.mjs` 中搜索 `progressLedger`、`toolEvidence`、`artifact summary`、`long task` 均返回 0 结果。

progress ledger 的注入路径是通过 `recordContextWorkMemory` 写入 work memory carryover，而非直接在 system prompt 中注入。这意味着：
- 压缩后 progress ledger 是否能被模型看到，取决于 carryover 的 token budget 和过滤逻辑。
- 文档要求的「压缩后**必须**注入」变成了「有机会被 carryover 带入」，可靠性降低。

**风险**：长任务压缩后，progress ledger 可能因为 carryover 预算不足而被截断，模型会丢失当前进度。

### Finding 4 — UI 可观测性无法验证

文档 P0 验收标准：「用户不看日志也知道何时压缩」「token meter 显示窗口来源、有效阈值、压缩前后值」。

`bridge/ipc.mjs:351` 有 `context_compression` 事件发送，但前端 ChatView / MainWindowApp 的实现不在当前审查范围内，无法确认 UI 是否持久展示了压缩事件。

---

## 已完成的部分（确认有效）

| 能力 | 状态 | 关键实现 |
|---|---|---|
| **Context Budget Ledger** | ✅ 完成 | `resolveContextWindowInfo` 返回 `windowSource`，`buildContextCompressionBudget` 包含完整预算分项，`agent.mjs` 和 `providers.mjs` 均传递 `windowSource` |
| **Compression 事件** | ✅ 完成 | `agent.mjs:366` 发 `context_compression` 事件，`ipc.mjs:351` 转发到前端 |
| **Progress Ledger 工具** | ✅ 完成 | `update_progress` / `read_progress` 工具定义完整，含 goal/phase/completed/pending/nextAction/artifactRefs 等字段，通过 work memory 持久化 |
| **Tool Evidence 收集** | ✅ 完成 | `autoToolEvidence` 记录工具执行证据，`buildToolEvidenceSummary` 生成摘要 |
| **Artifact Store** | ✅ 完成 | `create_artifact` / `append_artifact_chunk` / `read_artifact_slice` / `summarize_artifact` 均已实现，含 `MAX_ARTIFACT_CHUNKS=500` 和 `MAX_ARTIFACT_SLICE_LIMIT=20` 限制 |
| **Assistant Output Spillover** | ✅ 完成 | `spillRuntimeArtifact` + `providers.mjs` 中间轮 spillover 逻辑，`MAX_SPILLED_ARTIFACT_TEXT_CHARS=8000` 阈值 |
| **Compression Budget 计算** | ✅ 完成 | 压缩预算计入 system/tool/output/buffer，阈值计算包含 `compressionThresholdTokens * 0.85` |

---

## 总结

**7 项中 5 项确认落地，2 项有明确缺口，1 项（UI）无法从当前代码验证。**

最需要优先修复的是 **Repeat Read Guard**（Finding 1）——这是长任务防滚雪球的核心机制，文档声称已完成但代码中不存在。其次是确认 `appendRuntimeToolEvidenceToSystemPrompt` 的导入链路（Finding 2）和 progress ledger 的注入可靠性（Finding 3）。





好，基于完整的代码追踪，你报告的两个现象的根因如下：

---

## 问题 1：前端看不到压缩发生在「思考中」

**根因：IPC 事件发了，但前端没有接收。**

链路追踪：

- `bridge/agent.mjs:356` → 调用 `hooks?.onContextCompression?.(contextCompression)` ✅
- `bridge/ipc.mjs:349-351` → `emit({ type: 'context_compression', contextCompression })` ✅
- `src/MainWindowApp.tsx:861-905` → `contextCompressionEvent()` 函数存在，能从 snapshot 构造 `MessageEvent` ✅

**但是**：前端的 `listen()` 只监听了 `tauri://focus` 和 `settings:updated`，**没有任何 handler 接收 `context_compression` IPC 事件**。我搜遍了 `src/` 下所有文件，`context_compression` 仅出现在 `MainWindowApp.tsx:890` 的 `contextCompressionEvent()` 函数内部（作为字段值），而不是作为事件监听目标。

所以实际效果是：
- bridge 侧压缩完成 → IPC 事件发出 → **前端静默丢弃**
- 只有当 `AgentTaskSnapshot.contextCompression` 字段被同步更新后，`contextCompressionEvent()` 才可能在构建 message events 时被调用到

**修复方向**：在 `MainWindowApp.tsx` 中增加对 `context_compression` 事件的 `listen`，收到后更新 session 的 `contextCompression` 状态，并在当前 assistant message 的 events 中插入压缩卡片。

---

## 问题 2：模型仍然大段输出思考内容

**根因：只靠 prompt 约束，没有 runtime 硬约束。**

文档 P3 明确要求：

> reasoning delta 只展示/归档，不作为下一轮事实源。  
> provider loop 检测中间 assistant content 长度，超阈值时保存为 artifact，transcript 替换为摘要。

但代码中：

- `bridge/agent.mjs:368-381` — 压缩时通过 `onReasoningDelta` 发了一段 summary 文本，但这只是一条一次性摘要，不是持续的 reasoning 长度控制
- **没有** reasoning content 的 token 长度检测
- **没有** reasoning spillover（`spillRuntimeArtifact` / `MAX_SPILLED_ARTIFACT` 在 bridge 中搜索结果为 0）
- 压缩后的 system prompt 中**没有**注入 progress ledger（`agentPrompting.mjs` 中搜 `progressLedger` 为 0）

结果：压缩后模型看到的只有压缩摘要文本，没有结构化的进度账本，自然会重新用长篇思考来「回忆」之前做到哪了。

**修复方向**：
1. 在 `providers.mjs` 的 provider loop 中，对 reasoning delta 做 token 计数，超过阈值时自动截断并保存为 artifact
2. 压缩后在 system prompt 中注入 progress ledger（需要在 `agentPrompting.mjs` 中实现）
3. 这两步配合才能打断「压缩 → 遗忘 → 长思考 → 再压缩」的循环

---

## 一句话总结

两个问题的核心都是 **「事件发了但没人接」** 和 **「只有软约束没有硬约束」**——压缩机制本身跑通了，但前端没有消费压缩事件，runtime 也没有对 reasoning 长度做截断。